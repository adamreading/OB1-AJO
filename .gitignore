node_modules/
.DS_Store
.planning/
# Maintainer-local overlays (not part of upstream contribution contract)
.local/
.agent/
.claude.json
.claude/worktrees/
.claude/settings.json
.claude/settings.local.json

# Python caches
__pycache__/
*.pyc

# Credentials & runtime state
supabase/.temp/
.env
.env.local
.env.*.local
credentials.json
token.json
gmail-sync-log.json
sync-log.json
scripts/.plaud-processed.json
scripts/edge-snapshots/
.claude/scheduled_tasks.lock
start_brain.old

# Plaud curator state — contains personal data (colleague names, SCL internals,
# processed Plaud recording IDs). Schemas documented in CLAUDE.md.
processor_prompt/

# Build artifacts
deno.lock

# Local wiki output — never commit generated wiki files
wikis/
compiled-wiki/
output/
